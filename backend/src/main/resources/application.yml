server:
  port: ${PORT:8080}

spring:
  application:
    name: pitstop-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/pitstop}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000
      idle-timeout: 600000

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        default_schema: public

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    url: ${DB_URL:jdbc:postgresql://localhost:5432/pitstop}
    user: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 12MB

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=300s

app:
  jwt:
    secret: ${JWT_SECRET}
    access-token-expiry-ms: 900000
    refresh-token-expiry-ms: 604800000
    issuer: pitstop.manutex.com

  storage:
    endpoint: ${STORAGE_ENDPOINT:}
    bucket: ${STORAGE_BUCKET:pitstop-docs}
    region: ${AWS_REGION:us-east-1}
    presigned-url-expiry-minutes: 5

  rate-limit:
    capacity: 60
    refill-tokens: 60
    refill-duration-seconds: 60

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized

togglz:
  enabled: true
  feature-enums: com.manutex.pitstop.config.AppFeatures
  console:
    enabled: true
    path: /admin/toggles
    secured: true

logging:
  level:
    root: WARN
    com.manutex.pitstop: INFO
    org.springframework.security: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
